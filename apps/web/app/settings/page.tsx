import { RoutePlaceholder } from "../_components/route-placeholder";

export default function SettingsPage() {
  return (
    <RoutePlaceholder
      title="Settings"
      purpose="Manager-only configuration for regions, monitors, billing types, free customers, and tariffs."
    />
  );
}
