import { RoutePlaceholder } from "../_components/route-placeholder";

export default function CustomersPage() {
  return (
    <RoutePlaceholder
      title="Customers"
      purpose="Searchable customer list with role-based edit permissions and quick links to billing/payment history."
    />
  );
}
