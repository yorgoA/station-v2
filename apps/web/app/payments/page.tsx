import { RoutePlaceholder } from "../_components/route-placeholder";

export default function PaymentsPage() {
  return (
    <RoutePlaceholder
      title="Payments"
      purpose="Employee payment recording flow with receipt upload and recent-payment safety checks."
    />
  );
}
